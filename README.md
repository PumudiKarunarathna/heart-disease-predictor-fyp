# heart-disease-predictor-fyp
heart prediction web app

Run npm install from the root folder to install all dependencies.

Start the frontend by running npm run dev from the root folder.

Open a new terminal, navigate to the backend folder by running cd server, then start the backend using npm start.
